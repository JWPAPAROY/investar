# 자기주식취득 = 강제 흐름 축 판정 기준 (사전 등록)

> **작성 2026-09-07. 새 데이터를 보기 전에 확정한다.**
>
> 이 문서는 [DISCLOSURE_VERDICT.md](./DISCLOSURE_VERDICT.md)의 후속이자 **별개의 판정**이다.
> 앞 라운드에서 A2_자기주식취득은 ①(+0.54%p)을 통과하고 ②에서 기각됐다 —
> 다만 그 기각은 실질이 아니라 **공식 결함**이었다(사전 드리프트가 음수라 잔여비율의
> 분모 부호가 뒤집혔다). 그 결함을 알고도 앞 라운드에서는 고치지 않았다.
> 성적을 본 뒤 공식을 고치면 그게 골대 옮기기이기 때문이다.
>
> 그래서 여기서는 **같은 효과를 다시 판정하지 않는다.** 그건 순환논증이다.
> 대신 **왜 그 효과가 존재하는가**를, 앞 라운드에서 한 번도 보지 않은 차원으로 검정한다.

---

## 가설

> **자기주식취득 결정의 초과수익은 정보가 아니라 강제 매수 흐름에서 나온다.**

자기주식취득은 "회사가 앞으로 N개월간 자기 주식 X원어치를 사겠다"는 예고다.
새로운 정보가 아니라 **확정된 매수 주체의 등장**이다. 이 가설이 맞으면 앞 라운드의
두 관측이 동시에 설명된다:

| 관측 | 흐름 가설의 설명 |
|---|---|
| 사전 드리프트가 없다 (D-1 누적 **−0.76%**) | 유출될 정보가 애초에 없다. 이사회 결의 즉시 공시 |
| 공시 후에도 계속 오른다 (D+5 **+3.32%**) | 회사가 실제로 그 기간 동안 사고 있다 |

이것이 사실이라면 자기주식취득은 "덜 새어나간 공시"가 아니라
**지수 리밸런싱·보호예수 해제와 같은 범주** — 즉 강제 흐름 축의 첫 실물 사례다.
정보 우위가 필요 없으므로 사전 유출 문제에 원천적으로 면역이다.

---

## 앞 라운드와 무엇이 다른가 (이중 사용 방지)

같은 데이터에서 같은 것을 두 번 재면 안 된다. 아래 다섯 차원은
**앞 라운드에서 한 번도 계산하지 않았고, 판정에도 쓰이지 않았다.**

| 차원 | 앞 라운드 | 이번 |
|---|---|---|
| 취득 예정 **금액/시총** | 미수집 | P1 |
| 취득 **방법**(직접/신탁) | 미수집 | P2 |
| 취득 **기간** | 미수집 | P3 |
| 취득 **목적** | 미수집 | P5(병기) |
| **결과보고서** 대조군 | 분류에서 제외만 | P4 |

앞 라운드가 확립한 것("효과가 존재한다")은 전제로 두고, 이번은 그 **원인**만 묻는다.
따라서 "효과 크기가 +0.5%p를 넘는가"는 여기서 다시 판정하지 않는다.

---

## 대상

| | 값 |
|---|---|
| 이벤트 | `A2_자기주식취득` (직접취득 + 신탁계약체결), 정정·자회사 제외 |
| 기간 | 2022-01-01 ~ 2026-08-21 (KRX 가격 데이터 범위) |
| 표본 | 2,293건 / 신호일 906일 / 독립블록 227 (앞 라운드 실측) |
| 상세 | DART `tsstkAqDecsn` · `tsstkAqTrctrCcDecsn` 구조화 API |
| 대조군 | `자기주식취득결과보고서` (앞 라운드에서 유형 제외된 것) |
| 척도 | **추정량 C** — 창 수익 − 같은 창을 온전히 관측한 동일일·동일 시총분위 동료의 중앙값 |
| 진입·지평 | D+1 종가 매수 → D+5 종가 매도 (앞 라운드와 동일, 변경 없음) |

### 수집할 필드 (`tsstkAqDecsn` 실측 확인 완료)

```
aqpln_prc_ostk   취득 예정 금액(보통주)     → P1 분자
aqpln_stk_ostk   취득 예정 주식수
aqexpd_bgd/edd   취득 예상 기간 시작/종료   → P3
aq_pp            취득 목적                  → P5
aq_mth           취득 방법                  → P2
d1_prodlm_ostk   1일 매수주문 수량 한도     → 참고(흐름 강도의 상한)
```

---

## 척도 게이트 (P0) — 이것부터 통과해야 나머지를 본다

앞 라운드 1회차가 통제군 −0.69%p로 무효가 된 전례가 있다. 매 실행마다 확인한다.

- 무작위 위약(12만 건) 편향 **|중앙값| ≤ 0.1%**
- 통제군 `Z1_정기보고서` 매칭초과 **|x| ≤ 0.3%p**

하나라도 벗어나면 **판정을 출력하지 않는다.**

---

## 예측과 판정 기준

방향과 임계값을 **데이터를 보기 전에** 못 박는다.

### P4 — 반증 게이트 (최우선, 이것이 무너지면 전부 기각)

**대상: `자기주식취득결과보고서`의 D+1→D+5 매칭초과 중앙값**

결과보고서는 **이미 매수를 끝낸 뒤**에 내는 공시다. 강제 흐름이 원인이라면
여기엔 남아 있는 매수가 없으므로 **효과가 없어야 한다.**

- **|x| ≤ 0.3%p** → 통과 (흐름 가설 유지)
- **|x| > 0.3%p** → **전체 기각.** 흐름이 아니라 다른 것이 원인이다

> 이 시험을 맨 앞에 두는 이유: 나머지 셋은 전부 "흐름이면 이래야 한다"는 확인이고,
> P4만이 "흐름이 아니면 이렇게 나온다"는 **반증**이다. 반증 가능한 예측이 없는
> 가설은 검정한 것이 아니다.

### P1 — 용량-반응 (주 판정)

**취득 예정 금액 ÷ 신호일 시가총액**으로 5분위. 분위별 D+1→D+5 매칭초과.

- **Q5 − Q1 ≥ +1.0%p** 이고 **분위 순서 Spearman ρ ≥ +0.8** → 통과
- 그 외 → **기각**

살 물량이 클수록 밀어올리는 힘이 커야 한다. 단조성을 함께 요구하는 이유는
Q5 하나만 튀는 것은 소수 종목의 운일 수 있기 때문이다(저PBR 5분위 단조성 판정과 같은 논리).

### P2 — 실행 경로

**직접취득 vs 신탁계약체결**의 D+1→D+5 매칭초과 차이.

- 직접취득은 3개월 내 시장에서 **즉시** 매수한다.
- 신탁계약은 6개월~1년 계약이고 위탁 중개업자가 재량껏 분산 매수한다.
- 따라서 **단기(D+5)에는 직접취득이 더 커야 한다.**

- **직접 − 신탁 ≥ +0.5%p** → 통과
- 그 외 → 기각 (부호가 반대면 흐름 가설에 불리한 증거)

### P3 — 지속성

- **취득 기간 중**(`aqexpd_bgd` ~ `aqexpd_edd`): D+1→D+20 순초과 **≥ 0**(비용 차감 후) → 통과
- **취득 기간 종료 후** 20거래일 매칭초과 **≤ +0.2%p** → 소멸 확인

둘 다 만족해야 P3 통과. 흐름이 원인이면 **흐름이 멈출 때 효과도 멈춰야 한다.**

### P5 — 목적 분해 (병기, 판정 기준 아님)

선행연구는 주가안정·이익소각 목적은 (+), 임직원 인센티브 목적은 (−)라고 보고했다.
`aq_pp`로 갈라 재현되는지 본다. **통과/기각에 쓰지 않는다** — 사전에 방향을 확정할
만큼 근거가 두텁지 않고, 여기에 임계값을 두면 다중검정이 늘어난다.

---

## 채택 조건

```
P0 통과  AND  P4 통과  AND  P1 통과  AND  (P2 또는 P3 중 최소 1개)
```

- **P4 실패** → 전체 기각. 흐름 가설 폐기, 자기주식취득 축 종료
- **P1 실패** → 기각. 용량과 무관하다면 흐름이 원인이 아니다
- **P1 통과 + P2·P3 둘 다 실패** → **기각.** 용량-반응 하나만으로는 시총 효과의 재탕일 수 있다

애매 구간을 두지 않는다. "조금만 더 보자"가 이 저장소의 고질이다.

---

## 앞 라운드의 명세 결함 — 여기서 미리 고친다

**잔여비율(사후/사전)은 사전 드리프트가 0 이하일 때 의미가 없다.**
사전이 음수면 분모 부호가 뒤집혀 좋은 패턴이 큰 음수로 나온다.

이 판정에서는:
- **사전 드리프트 ≤ 0 이면 잔여비율 조건을 면제한다.** 사전에 새어나간 것이 없으면
  "잔반인가"라는 질문 자체가 성립하지 않는다.
- 사전 드리프트 > 0 일 때만 잔여비율 ≥ 0.5 를 요구한다.

이것은 앞 라운드 결과를 뒤집는 조항이 아니다. 앞 라운드의 A2 기각은 그대로 유지된다.

---

## 중단 조건 (판정 실행 전이라도)

하나라도 걸리면 멈추고 원인부터 본다.

- 척도 게이트 P0 이탈
- 상세 필드 확보율 < 80% (`tsstkAqDecsn` 매칭 실패)
- P1 분위당 표본 < 100건
- 독립블록 < 20
- 취득 예정 금액이 결측이거나 0인 건이 전체의 20% 초과

---

## 실행 규율

**판정 실행은 1회다.** 과거 데이터가 이미 존재하므로 기다릴 것이 없고,
위험은 오직 "좋아 보일 때까지 다시 돌려보는 것"이다.

- 예측(P1~P4)·방향·임계값은 **이 문서 작성 시점에 확정됐다**
- 실행 후 하나라도 바꾸면 판정을 리셋하고 아래 변경 이력에 남긴다
- `scripts/buyback-verdict.js`가 위 값을 **코드로** 계산한다. 눈으로 고르지 않는다
- 사후 슬라이스(업종별·연도별·시총별 재탐색)를 추가하지 않는다.
  추가하려면 판정을 리셋하고 새 사전 등록 문서를 쓴다

### 강건성 병기 (판정 기준 아님)

전후반 분할 / 레짐(BULL·BEAR·FLAT) / 시총 5분위 / D0 진입 참고값 / P5 목적 분해.

---

## 판정 결과별 행동

| 결과 | 행동 |
|---|---|
| 채택 | 강제 흐름 축 확정. **다음 사례로 지수 리밸런싱·보호예수 해제를 같은 틀로 검정.** 실매매는 P1의 상위 분위(대규모 매입)로 좁혀 재검토 |
| P4 실패 | 흐름 가설 폐기. 자기주식취득의 효과는 정보 반응이므로 사전 유출 문제로 되돌아간다 → 축 종료 |
| P1 실패 | 기각. A2는 앞 라운드 ② 기각과 함께 최종 종료 |
| P1 통과 + P2·P3 실패 | 기각. 단 "용량-반응은 있었다"를 기록하고, 다음 사전 등록에서 다른 메커니즘 가설로 재설계 |

**어느 결과든 수집한 상세 데이터는 강제 흐름 축의 다음 사례에서 재사용된다.**

---

## 알려진 한계 (판정 전에 적어둔다)

- **효과 크기의 마진이 얇다.** 앞 라운드 A2 순초과는 +0.54%p로 통과선(+0.5) 바로 위였다.
  P1이 통과하면 상위 분위의 마진은 더 두꺼울 것으로 기대하지만, 그것은 예측이지 사실이 아니다.
- **신탁계약(`tsstkAqTrctrCcDecsn`)은 삼성전자 표본에서 013(데이터 없음)이었다.**
  경로가 없는 것이 아니라 그 회사에 이력이 없는 것으로 보이나, 수집 시 확인이 필요하다.
  확보 실패 시 **P2는 "측정 불가"로 처리**하고 P3로만 판정한다(P1은 여전히 필수).
- **권리락은 이 축에서는 문제가 되지 않는다.** 자기주식취득은 주식수를 늘리지 않는다.
  다만 취득 기간 중 다른 사유의 권리락이 섞일 수 있으므로 상장주식수 변동일 제외는 유지한다.

---

## 변경 이력

| 날짜 | 변경 | 이유 |
|---|---|---|
| 2026-09-07 | 최초 작성 | 상세 데이터 수집 전. `tsstkAqDecsn` 필드 존재만 확인하고 값은 보지 않았다 |
| 2026-09-07 | 수집 완료 | DART 상세 1,988건 확보(2,350 중 84.6%). 직접 746 / 신탁 1,242. 취득금액 결측 2.1%. 중단 조건 3개 모두 통과 |
| 2026-09-07 | **판정 실행 (1회, 확정) — 기각** | P0 ✅(위약 +0.00% / 통제군 +0.01%p) · P4 ✅(결과보고서 +0.04%p, n=870) · P1 ✅(Q1 +0.09 → Q5 +2.22, Q5−Q1 **+2.13%p**, Spearman **1.00**) · P2 ❌(직접 +0.78 vs 신탁 +1.24, **부호가 예측과 반대**) · P3 ❌(취득기간 중 순초과 +1.82%p 통과였으나 기간 종료 후 +0.38%p로 기준 +0.20 초과). 채택 조건 `P0 AND P4 AND P1 AND (P2 또는 P3)` 미충족 → **기각** |
| 2026-09-07 | ⚠️ 사후 발견된 **P2·P3 명세 결함**(미수정) | **P2**: 신탁계약 금액(`ctr_prc`)은 6~12개월 계약 총액이고 직접취득(`aqpln_prc_ostk`)은 3개월 한도라, 신탁이 체계적으로 클 수 있다. 그렇다면 P2의 부호는 **실행 속도가 아니라 규모(P1)가 결정**한 것이며, 내 예측이 규모를 통제하지 않았다. **P3**: "기간 종료 후 20거래일"이 흐름 없는 구간이라는 전제가 약하다 — 계약금액 소진 후 곧바로 새 취득을 결의하는 롤링 프로그램이 실측 표본에 있었다(중도해지 사유 "취득 완료(계약금액 소진)"). 둘 다 **고치지 않는다.** 성적을 본 뒤 명세를 고치면 골대 옮기기다 |
