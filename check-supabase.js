require('dotenv').config({path: 'c:/Users/knoww/investar/.env'}); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.SUPABASE_ANON_KEY); supabase.from('screening_recommendations').select('*').eq('recommendation_date', '2026-03-05').in('stock_name', ['흥아해운', 'S-Oil', '대한해운']).then(({data, error}) => console.log('DB Data:', data || error));
