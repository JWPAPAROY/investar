# 환경변수
.env

# Node.js
node_modules/
npm-debug.log
yarn-error.log

# 에디터
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# 로그
*.log
logs/

# 데이터 파일 (실시간 추적 데이터)
data/*.json
!data/.gitkeep

# Debug & Test Scripts
debug-*.js
test-*.js
discover-*.js
check-*.js
find-*.js
refresh-*.js

# Vercel
.vercel
.env.backup

.vercel

# 한국투자증권 API 참고 문서 (배포 제외)
api/koreainvestment/

# 로컬 개발/조사 스크립트
scripts/inspect-schema.js
scripts/top1-recent.js

# 대용량 데이터 스냅샷 (v3.96 추가)
#   data/*.json 만 막혀 있어서 .jsonl 240MB(krx-daily 176M + price-history 52M + krx-index 12M)가
#   `git add -A` 한 번이면 레포에 들어갈 수 있었다. 재생성 가능한 수집물이므로 추적하지 않는다.
#   (collect-krx-daily.js / collect-price-history.js 로 다시 만들 수 있다)
data/*.jsonl
!data/lowvol-picks.jsonl
